# Local override for run-experiment.sh. Copy to .env and edit; .env is gitignored.

# Directory holding the prebuilt publisher/relay/subscriber binaries.
# Defaults to this directory.
# BIN_DIR=/path/to/bin
